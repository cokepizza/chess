const validation = (board, prev, next) => {
    //  validate checkMate
    const kingAxisWhite;
    for(let i=0; i<8; ++i) {
        for(let j=0; j<8; ++j) {

        }
    }

    //  validatae check status


};